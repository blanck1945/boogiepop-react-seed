# GitLab CI — imagen linux/arm64 → ECR (repo `boogiepop-remote`) y rollout ECS opcional.
# Contrato deploy: ../boogiepop-host/docs/DEPLOY-REMOTES.md
#
# Modelo de ramas: main → prod (+ tag :latest); develop → :develop; tags vX.Y.Z.
# MRs: solo lint (workflow evita pipeline duplicada rama+MR cuando hay MR abierta).
#
# Nota runners: usa Ubuntu + instalador oficial AWS CLI v2 (glibc). El paquete
# alpine `apk add aws-cli` puede romperse con pyexpat/libexpat en musl.
#
# Variables CI/CD (ejemplos Escenario B):
#   AWS_REGION=us-east-1
#   ECR_REPOSITORY=boogiepop-remote
#   VITE_REMOTE_BASE=https://mf.tudominio.com/
#   AWS_ROLE_ARN=arn:aws:iam::CUENTA:role/gitlab-ecr-ecs     # recomendado: OIDC
#   ECS_CLUSTER=boogiepop-api-cluster                         # opcional
#   ECS_SERVICE=boogiepop-api-fe-remote-svc                   # opcional (+ job manual)
#
# IAM trust (OIDC): proveedor GitLab.com, audiencia https://gitlab.com
# Runner: Docker-in-Docker; cliente desde Ubuntu docker.io debe alinear API con DinD.

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH

stages:
  - lint
  - publish
  - deploy

variables:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: boogiepop-remote
  VITE_REMOTE_BASE: "/"
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""

.docker_dind_ubuntu:
  image:
    name: ubuntu:22.04
    entrypoint: [""]
  services:
    # Debe coincidir con ubuntu docker.io (~29.x / API ≥1.52); DinD viejo ⇒ "client too new".
    - name: docker:29-dind
      command: ["--tls=false"]

# Instalador oficial AWS CLI v2 (GLIBC); sin paquete Alpine aws-cli frágil musl/expat.
.ubuntu_tools_aws_docker_publish: &ubuntu_tools_aws_docker_publish
  - |
    set -eu
    apt-get update -qq
    DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
      ca-certificates curl jq unzip docker.io
    case "$(uname -m)" in
      x86_64) AWS_CLI_ARCH=x86_64 ;;
      aarch64) AWS_CLI_ARCH=aarch64 ;;
      *)
        echo "Runner arquitectura no soportada para awscliv2 oficial: $(uname -m)"
        exit 1
        ;;
    esac
    curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-${AWS_CLI_ARCH}.zip" \
      -o /tmp/awscliv2.zip
    unzip -qo /tmp/awscliv2.zip -d /tmp
    rm -rf /usr/local/aws-cli 2>/dev/null || true
    /tmp/aws/install -i /usr/local/aws-cli -b /usr/local/bin --update
    aws --version
    PLUGIN_DIR=/usr/local/lib/docker/cli-plugins
    mkdir -p "$PLUGIN_DIR"
    if ! docker buildx version >/dev/null 2>&1; then
      case "$(uname -m)" in
        x86_64) BX=amd64 ;;
        aarch64) BX=arm64 ;;
        *) BX="$(uname -m)" ;;
      esac
      BUILDX_REL="v0.18.0"
      curl -fsSL \
        "https://github.com/docker/buildx/releases/download/${BUILDX_REL}/buildx-${BUILDX_REL}.linux-${BX}" \
        -o "$PLUGIN_DIR/docker-buildx"
      chmod +x "$PLUGIN_DIR/docker-buildx"
      export DOCKER_CLI_PLUGINS_PATH="/usr/local/lib/docker/cli-plugins"
    fi
    docker info

.aws_assume_role: &aws_assume_role
  - |
    if [ -n "${AWS_ROLE_ARN}" ] && [ -n "${GITLAB_OIDC_TOKEN:-}" ]; then
      CREDS=$(aws sts assume-role-with-web-identity \
        --role-arn "${AWS_ROLE_ARN}" \
        --role-session-name "gitlab-ci-${CI_PIPELINE_ID}" \
        --web-identity-token "${GITLAB_OIDC_TOKEN}" \
        --duration-seconds 3600 \
        --output json)
      export AWS_ACCESS_KEY_ID
      export AWS_SECRET_ACCESS_KEY
      export AWS_SESSION_TOKEN
      AWS_ACCESS_KEY_ID=$(echo "$CREDS" | jq -r '.Credentials.AccessKeyId')
      AWS_SECRET_ACCESS_KEY=$(echo "$CREDS" | jq -r '.Credentials.SecretAccessKey')
      AWS_SESSION_TOKEN=$(echo "$CREDS" | jq -r '.Credentials.SessionToken')
      export AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_SESSION_TOKEN
    elif [ -n "${AWS_ACCESS_KEY_ID:-}" ] && [ -n "${AWS_SECRET_ACCESS_KEY:-}" ]; then
      echo "AWS: autenticación con claves estáticas IAM (sin OIDC)."
    else
      echo "=============================================="
      echo "Falta autenticación AWS para este job."
      echo "Opciones (una sola rutina suficiente):"
      echo "  1) OIDC — Variable CI/AWS_ROLE_ARN + JWT del job (id_tokens en YAML)."
      echo "     Si AWS_ROLE_ARN está definido pero falla igual: el JWT llegó vacío (runner,"
      echo "     versión GitLab u OIDC IAM mal configurado)."
      echo "  2) Claves — Variables AWS_ACCESS_KEY_ID y AWS_SECRET_ACCESS_KEY (Masked)."
      echo ""
      echo "Variables marcadas sólo como Protected: sólo aplican si la pipeline corre en una"
      echo "rama o tag PROTEGIDO. Si ejecutás desde rama/feature sin proteger, vacíalas de"
      echo "Protected o protegé la referencia donde corre el job."
      echo "=============================================="
      if [ -n "${AWS_ROLE_ARN:-}" ] && [ -z "${GITLAB_OIDC_TOKEN:-}" ]; then
        echo "(Detectado AWS_ROLE_ARN pero GITLAB_OIDC_TOKEN vacío — revisá OIDC IAM o usar claves)."
      fi
      if [ -z "${AWS_ROLE_ARN:-}" ]; then
        echo "(AWS_ROLE_ARN no está definido en CI/CD Variables o no llega por Protected.)"
      fi
      exit 1
    fi
  - aws sts get-caller-identity
  - export AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
  - export ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"

.deploy_ecs_ubuntu_prep: &deploy_ecs_ubuntu_prep
  - |
    set -eu
    apt-get update -qq
    DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
      ca-certificates curl jq unzip
    case "$(uname -m)" in
      x86_64) AWS_CLI_ARCH=x86_64 ;;
      aarch64) AWS_CLI_ARCH=aarch64 ;;
      *)
        echo "Runner arquitectura no soportada: $(uname -m)"
        exit 1
        ;;
    esac
    curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-${AWS_CLI_ARCH}.zip" \
      -o /tmp/awscliv2.zip
    unzip -qo /tmp/awscliv2.zip -d /tmp
    rm -rf /usr/local/aws-cli 2>/dev/null || true
    /tmp/aws/install -i /usr/local/aws-cli -b /usr/local/bin --update
    aws --version

lint:
  stage: lint
  image: node:22-alpine
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm/
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run lint
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

docker_publish:
  extends: .docker_dind_ubuntu
  stage: publish
  id_tokens:
    GITLAB_OIDC_TOKEN:
      aud: https://gitlab.com
  before_script:
    - *ubuntu_tools_aws_docker_publish
    - *aws_assume_role
    - aws ecr describe-repositories --repository-names "${ECR_REPOSITORY}" --region "${AWS_REGION}"
    - aws ecr get-login-password --region "${AWS_REGION}" | docker login --username AWS --password-stdin "${ECR_REGISTRY}"
    - docker buildx create --use --name ci-arm --driver docker-container 2>/dev/null || docker buildx use ci-arm
    - docker buildx inspect --bootstrap
  script:
    - |
      TAGS="-t ${ECR_REGISTRY}/${ECR_REPOSITORY}:${CI_COMMIT_SHORT_SHA}"
      if [ -n "${CI_COMMIT_TAG}" ]; then
        TAGS="${TAGS} -t ${ECR_REGISTRY}/${ECR_REPOSITORY}:${CI_COMMIT_TAG}"
      fi
      if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
        TAGS="${TAGS} -t ${ECR_REGISTRY}/${ECR_REPOSITORY}:latest"
      fi
      if [ "${CI_COMMIT_BRANCH}" = "develop" ]; then
        TAGS="${TAGS} -t ${ECR_REGISTRY}/${ECR_REPOSITORY}:develop"
      fi
      echo "Building with VITE_REMOTE_BASE=${VITE_REMOTE_BASE}"
      docker buildx build --platform linux/arm64 --push \
        --build-arg "VITE_REMOTE_BASE=${VITE_REMOTE_BASE}" \
        --build-arg VITE_DEV_SERVER_ORIGIN=http://localhost:5173 \
        ${TAGS} \
        .
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
    - if: $CI_COMMIT_TAG =~ /^v[0-9]+\.[0-9]+\.[0-9]+/

# ECS: mismo rol OIDC/claves si el política incluye ecs:UpdateService.
deploy_ecs:
  stage: deploy
  image:
    name: ubuntu:22.04
    entrypoint: [""]
  id_tokens:
    GITLAB_OIDC_TOKEN:
      aud: https://gitlab.com
  needs: ["docker_publish"]
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "main"
      when: manual
    - if: $CI_COMMIT_BRANCH == "develop"
      when: manual
    - if: $CI_COMMIT_TAG =~ /^v[0-9]+\.[0-9]+\.[0-9]+/
      when: manual
  before_script:
    - *deploy_ecs_ubuntu_prep
    - *aws_assume_role
  script:
    - |
      if [ -z "${ECS_CLUSTER:-}" ] || [ -z "${ECS_SERVICE:-}" ]; then
        echo "Define ECS_CLUSTER y ECS_SERVICE en Variables CI/CD para lanzar rollout."
        exit 1
      fi
      aws ecs update-service --cluster "${ECS_CLUSTER}" --service "${ECS_SERVICE}" \
        --region "${AWS_REGION}" --force-new-deployment
